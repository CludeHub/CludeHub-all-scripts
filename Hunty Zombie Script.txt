local NEVERLOSE = loadstring(game:HttpGet("https://raw.githubusercontent.com/CludeHub/SourceCludeLib/refs/heads/main/NerverLoseLibEdited.lua"))()
local Loader = loadstring(game:HttpGet("https://raw.githubusercontent.com/CludeHub/SourceCludeLib/refs/heads/main/NL_LOADER.lua"))()

Loader:Load(false, "https://www.roblox.com/game-pass/1233310627/NEVERLOSE-CC-LOADER", 1233310627, function()
local Window = NEVERLOSE:AddWindow("CludeHub","Neverlose Menu V3",'original')
Window:AddTabLabel('Home')
local Credt = Window:AddTab('Home','home')
Window:AddTabLabel('Miscellaneous')
local Main = Window:AddTab('Main','list')
local Script = Window:AddTab('Script','code')

local combat = Main:AddSection('Combat',"left")
local esp = Main:AddSection('Esp',"left")
local tp = Main:AddSection('TP',"left")
local autofarm = Main:AddSection('Auto Farm',"right")
local player = Main:AddSection('Player',"right")
local misc = Main:AddSection('Misc',"right")

local antilag = Main:AddSection('Boost fps',"left")

local credit = Credt:AddSection('Credit',"left")
local other = Credt:AddSection('Other',"right")

local scriptleft = Script:AddSection('Hub',"left")
local scriptright = Script:AddSection('Hub',"right")

tp:AddButton("Call for help", function()

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

character:WaitForChild("HumanoidRootPart").CFrame = CFrame.new(136, -17, -57)
end)
-- 10 Left
scriptleft:AddButton("Nexor Hub", function()
    loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/3f4d1a2d8f9.lua"))()
end)

scriptleft:AddButton("BlackCat Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/ZciVd9LN"))()
end)

scriptleft:AddButton("Polluted Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/MvFqT8kQ"))()
end)

scriptleft:AddButton("Tora Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/0x6yBRbK"))()
end)

scriptleft:AddButton("Zombie Hunter Script", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/jE1vPz5R"))()
end)

scriptleft:AddButton("Hunty Zombie v1.3", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/SUGxA4GQ"))()
end)

scriptleft:AddButton("Ciel Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/pL7m8MyX"))()
end)

scriptleft:AddButton("Delta Hunty Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/rpdQ3ksF"))()
end)

scriptleft:AddButton("SpeedHub X", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/BWcQeF2g"))()
end)

scriptleft:AddButton("Axiora Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/1dRzUXQK"))()
end)



-- 10 Right
scriptright:AddButton("Tbao Hub", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/EXjYHYHz"))()
end)

scriptright:AddButton("No Key GUI", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/ycTPXh4K"))()
end)

scriptright:AddButton("HuntyZ Script 2025", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/fH7EYcWt"))()
end)

scriptright:AddButton("Universal Hunty GUI", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/WKgZxAkM"))()
end)

scriptright:AddButton("OP Hub v2", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/ZFSKHs7t"))()
end)

scriptright:AddButton("Zombie ESP Script", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/ccE4uAtL"))()
end)

scriptright:AddButton("Hunty Godmode", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/F5TQwPmj"))()
end)

scriptright:AddButton("Infinite Perk", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/d9VHg5ZC"))()
end)

scriptright:AddButton("Auto Farm Zombies", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/2rPTh2sV"))()
end)

scriptright:AddButton("Kill Aura Script", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/j4yR1NaU"))()
end)

-- REMOVE TEXTURES
antilag:AddButton("Remove Texture", function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Texture") or v:IsA("Decal") then
            v:Destroy()
        end
    end
    print("All textures removed")
end)

-- REMOVE PARTICLES
antilag:AddButton("Remove Particles", function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Fire") or v:IsA("Smoke") then
            v:Destroy()
        end
    end
    print("All particles removed")
end)

-- REMOVE FOG
antilag:AddButton("Remove Fog", function()
    local lighting = game:GetService("Lighting")
    lighting.FogEnd = 100000
    lighting.FogStart = 0
    lighting.FogColor = Color3.fromRGB(255, 255, 255)
    print("Fog removed")
end)

-- REMOVE GRAPHICS (Low graphics mode)
antilag:AddButton("Remove Graphics", function()
    -- Remove textures
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("Texture") or v:IsA("Decal") then
            v:Destroy()
        end
    end
    -- Remove particles
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Fire") or v:IsA("Smoke") then
            v:Destroy()
        end
    end
    -- Lower lighting
    local lighting = game:GetService("Lighting")
    lighting.GlobalShadows = false
    lighting.EnvironmentSpecularScale = 0
    lighting.EnvironmentDiffuseScale = 0
    lighting.FogEnd = 100000
    print("Graphics set to low")
end)

other:AddButton("Infinite yield",function()
loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)

credit:AddLabel("CludeHub Community")
credit:AddLabel("paulparasplaythis")

local spinningLucky = false
local spinningNormal = false

misc:AddToggle('Auto Spin Lucky Perk', false, function(val)
    spinningLucky = val
    if val then
        task.spawn(function()
            while spinningLucky do
                local args = {
                    1,
                    true
                }
                game:GetService("ReplicatedStorage"):WaitForChild("Packets"):WaitForChild("PerkSpin"):InvokeServer(unpack(args))
                task.wait(0.3)
            end
        end)
    end
end)

misc:AddToggle('Auto Spin Normal Perk', false, function(val)
    spinningNormal = val
    if val then
        task.spawn(function()
            while spinningNormal do
                local args = {
                    1,
                    [3] = true
                }
                game:GetService("ReplicatedStorage"):WaitForChild("Packets"):WaitForChild("PerkSpin"):InvokeServer(unpack(args))
                task.wait(0.3)
            end
        end)
    end
end)

local spinningLucky = false
local spinningNormal = false

misc:AddToggle('Auto Spin Lucky Weapon', false, function(val)
    spinningLucky = val
    if val then
        task.spawn(function()
            while spinningLucky do
                local args = {
                    2,
                    true
                }
                game:GetService("ReplicatedStorage"):WaitForChild("Packets"):WaitForChild("WeaponSpin"):InvokeServer(unpack(args))
                task.wait(0.3) -- delay so it doesn’t spam too fast
            end
        end)
    end
end)

misc:AddToggle('Auto Spin Normal Weapon', false, function(val)
    spinningNormal = val
    if val then
        task.spawn(function()
            while spinningNormal do
                local args = {
                    2,
                    [3] = true
                }
                game:GetService("ReplicatedStorage"):WaitForChild("Packets"):WaitForChild("WeaponSpin"):InvokeServer(unpack(args))
                task.wait(0.3)
            end
        end)
    end
end)

misc:AddButton('Redeem Codes', function()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Redeem = ReplicatedStorage:WaitForChild("Packets"):WaitForChild("RedeemCode")

    Redeem:InvokeServer("200KCCU")
    Redeem:InvokeServer("DAGGER")
    Redeem:InvokeServer("200KIN")
    Redeem:InvokeServer("NEWPLAYER1")
    Redeem:InvokeServer("400KLIKES")
end)

player:AddToggle('Infinity jump', false, function(val)
    local UserInputService = game:GetService("UserInputService")
    local player = game:GetService("Players").LocalPlayer
    local infiniteJumpEnabled = val
    
    UserInputService.JumpRequest:Connect(function()
        if infiniteJumpEnabled and player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end)

local Players = game:GetService("Players")
local lplr = Players.LocalPlayer -- use this for Roblox's LocalPlayer
local character = lplr.Character or lplr.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local stats = {
    WalkSpeed = 16,
    JumpPower = 50,
    FOV = 70,
    Gravity = workspace.Gravity,
    MaxZoom = 128
}

player:AddSlider("WalkSpeed",1,200,16,function(val)
    stats.WalkSpeed = val
end)

player:AddSlider("JumpPower",1,200,50,function(val)
    stats.JumpPower = val
end)

player:AddSlider("FOV",30,120,70,function(val)
    stats.FOV = val
end)

player:AddSlider("Gravity",0,500,workspace.Gravity,function(val)
    stats.Gravity = val
end)

player:AddSlider("MaxZoom",10,1000,128,function(val)
    stats.MaxZoom = val
end)

player:AddButton("Apply Stats",function()
    humanoid.WalkSpeed = stats.WalkSpeed
    humanoid.JumpPower = stats.JumpPower
    workspace.CurrentCamera.FieldOfView = stats.FOV
    workspace.Gravity = stats.Gravity
    lplr.CameraMaxZoomDistance = stats.MaxZoom
end)

-- Noclip Toggle
local noclipConn
player:AddToggle("Noclip", false, function(val)
    if val then
        noclipConn = game:GetService("RunService").Stepped:Connect(function()
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end)
    else
        if noclipConn then
            noclipConn:Disconnect()
            noclipConn = nil
        end
    end
end)

--// Realistic ESP for Roblox Exploits
--// Example UI usage with your system

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local ESP_ENABLED = false
local SHOW_HEALTH = true
local SHOW_NAME = true
local SHOW_DISTANCE = true
local SHOW_BOX = true
local BOX_COLOR = Color3.fromRGB(255,255,255)

--// ESP Drawing Handler
local function CreateESP(player)
    if player == LocalPlayer then return end
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local draw = {
            Box = Drawing.new("Square"),
            Name = Drawing.new("Text"),
            HealthBar = Drawing.new("Line"),
            Distance = Drawing.new("Text")
        }
        draw.Box.Thickness = 2
        draw.Box.Filled = false
        draw.Box.Color = BOX_COLOR
        draw.Box.Visible = false

        draw.Name.Size = 13
        draw.Name.Color = Color3.fromRGB(255,255,255)
        draw.Name.Center = true
        draw.Name.Outline = true
        draw.Name.Visible = false

        draw.HealthBar.Thickness = 2
        draw.HealthBar.Color = Color3.fromRGB(0,255,0)
        draw.HealthBar.Visible = false

        draw.Distance.Size = 13
        draw.Distance.Color = Color3.fromRGB(200,200,200)
        draw.Distance.Center = true
        draw.Distance.Outline = true
        draw.Distance.Visible = false

        return draw
    end
end

local espObjects = {}

--// Update ESP
RunService.RenderStepped:Connect(function()
    for _,player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local char = player.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")

            if hrp and hum and ESP_ENABLED then
                if not espObjects[player] then
                    espObjects[player] = CreateESP(player)
                end
                local esp = espObjects[player]
                local pos, vis = Camera:WorldToViewportPoint(hrp.Position)
                if vis then
                    local scale = (1 / (pos.Z)) * 1000
                    local sizeX, sizeY = 4 * scale, 5 * scale

                    -- Box
                    if SHOW_BOX then
                        esp.Box.Size = Vector2.new(sizeX, sizeY)
                        esp.Box.Position = Vector2.new(pos.X - sizeX/2, pos.Y - sizeY/2)
                        esp.Box.Visible = true
                    else
                        esp.Box.Visible = false
                    end

                    -- Name
                    if SHOW_NAME then
                        esp.Name.Position = Vector2.new(pos.X, pos.Y - sizeY/2 - 12)
                        esp.Name.Text = player.Name
                        esp.Name.Visible = true
                    else
                        esp.Name.Visible = false
                    end

                    -- Health
                    if SHOW_HEALTH then
                        local hpPercent = hum.Health / hum.MaxHealth
                        esp.HealthBar.From = Vector2.new(pos.X - sizeX/2 - 4, pos.Y + sizeY/2)
                        esp.HealthBar.To = Vector2.new(pos.X - sizeX/2 - 4, pos.Y + sizeY/2 - sizeY * hpPercent)
                        esp.HealthBar.Visible = true
                    else
                        esp.HealthBar.Visible = false
                    end

                    -- Distance
                    if SHOW_DISTANCE then
                        local dist = math.floor((hrp.Position - Camera.CFrame.Position).Magnitude)
                        esp.Distance.Text = tostring(dist).."m"
                        esp.Distance.Position = Vector2.new(pos.X, pos.Y + sizeY/2 + 12)
                        esp.Distance.Visible = true
                    else
                        esp.Distance.Visible = false
                    end
                else
                    for _,v in pairs(esp) do
                        v.Visible = false
                    end
                end
            end
        end
    end
end)

--// UI Integration
esp:AddToggle("Enable ESP", false, function(val)
    ESP_ENABLED = val
end)

esp:AddToggle("Show Boxes", true, function(val)
    SHOW_BOX = val
end)

esp:AddToggle("Show Health", true, function(val)
    SHOW_HEALTH = val
end)

esp:AddToggle("Show Names", true, function(val)
    SHOW_NAME = val
end)

esp:AddToggle("Show Distance", true, function(val)
    SHOW_DISTANCE = val
end)

esp:AddDropdown("Box Color", {"White","Red","Green","Blue"}, "White", function(val)
    local colors = {
        White = Color3.fromRGB(255,255,255),
        Red = Color3.fromRGB(255,0,0),
        Green = Color3.fromRGB(0,255,0),
        Blue = Color3.fromRGB(0,0,255)
    }
    BOX_COLOR = colors[val] or Color3.fromRGB(255,255,255)
end)
local autoAttack = false
local attackLoop

combat:AddToggle('Kill Aura', false, function(val)
    autoAttack = val

    if autoAttack then
        attackLoop = task.spawn(function()
            while autoAttack do
                local args = {
                    buffer.fromstring("\b\004\000")
                }
                game:GetService("ReplicatedStorage"):WaitForChild("ByteNetReliable"):FireServer(unpack(args))
                task.wait(0.1) -- adjust delay if needed
            end
        end)
    else
        autoAttack = false
    end
end)

-- Auto Perks
do
    local running = false
    combat:AddToggle('Auto Perks', false, function(val)
        running = val
        if val then
            task.spawn(function()
                while running do
                    local args = { buffer.fromstring("\f") }
                    game:GetService("ReplicatedStorage"):WaitForChild("ByteNetReliable"):FireServer(unpack(args))
                    task.wait(0.01)
                end
            end)
        end
    end)
end

-- Auto Tap
do
    local running = false
    combat:AddToggle('Auto Tap', false, function(val)
        running = val
        if val then
            task.spawn(function()
                while running do
                    local args = { buffer.fromstring("\b\a\000") }
                    game:GetService("ReplicatedStorage"):WaitForChild("ByteNetReliable"):FireServer(unpack(args))
                    task.wait(0.01)
                end
            end)
        end
    end)
end

-- Auto Skill
do
    local running = false
    combat:AddToggle('Auto Skill', false, function(val)
        running = val
        if val then
            task.spawn(function()
                while running do
                    local rs = game:GetService("ReplicatedStorage"):WaitForChild("ByteNetReliable")

                    rs:FireServer(unpack({ buffer.fromstring("\b\003\000") }))
                    rs:FireServer(unpack({ buffer.fromstring("\b\006\000") }))
                    rs:FireServer(unpack({ buffer.fromstring("\b\005\000") }))

                    task.wait(0.01)
                end
            end)
        end
    end)
end

local RunService = game:GetService("RunService")
local farming = false
local farmConnection

autofarm:AddToggle('Enable Auto Farm', false, function(val)
    farming = val
    if farming then
        farmConnection = RunService.Heartbeat:Connect(function()
            local zombies = {}
            for _, mob in pairs(game.Workspace.Entities.Zombie:GetChildren()) do
                if mob:IsA("Model") and mob:FindFirstChild("HumanoidRootPart") then
                    table.insert(zombies, mob)
                end
            end

            if #zombies > 0 and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local target = zombies[math.random(1, #zombies)]
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 3, 0)
            end
            task.wait(1)
        end)
    else
        if farmConnection then
            farmConnection:Disconnect()
            farmConnection = nil
        end
    end
end)
end)